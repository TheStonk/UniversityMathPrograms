import numpy as np

a = np.array([[0,1,0],
              [0,1,0]])
b = np.array([[1,1],
              [0,1]])
#print(a@b) # multiply two matrix, use @ for matrix mul
print(a.transpose()) # Transpose matrix

